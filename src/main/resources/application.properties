spring.datasource.url=jdbc:postgresql://localhost:5432/blogs
spring.datasource.username=schias
spring.datasource.password=sOmeSecr3t!

# Enable SQL logging (optional)
spring.jpa.show-sql=true

# Hibernate DDL auto configuration (optional)
spring.jpa.hibernate.ddl-auto=update

# PostgreSQL dialect for Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Enable the health endpoint
management.endpoints.web.exposure.include=health,info

# Include database health in the health check
management.health.db.enabled=true

# Optional: Enable detailed health info
management.endpoint.health.show-details=always
